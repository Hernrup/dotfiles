# Tell tmux to always expect 256 colors
alias tmux='tmux -2'

# attach to an exisiting tmux session
alias tma='tmux attach'

# create new session for a secondary display and attach
alias tmc='tmux new-session -t DEF'
